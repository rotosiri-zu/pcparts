.album.py-5
  .container
    .row
      .post
      - @items.each do |item|
        .col-lg-3.col-md-6
          .card
            = link_to item.item_url do
              = image_tag item.image_url, class: "card-img-top"
            .card-body
              %p.card-title= link_to (item.item_name)
              %span.card-price= "¥#{item.item_price.to_s(:delimited)}"
              .star