- breadcrumb :category_show
.container
  %h1.categorytitle= "#{@category.name}の商品一覧"
  .album.py-5
    %h1.newitems
    .container
      .row
        .post
        - @posts.each do |post|
          .col-lg-3.col-md-6
            .card
              = image_tag post.image.url, class: "card-img-top"
              .card-body
                %p.card-title= link_to (post.title), post
                %p.card-genre= (post.category.name)
                %span.card-price= "¥#{post.price.to_s(:delimited)}" 
                .star
                %span{id: "star-rate-#{post.id}"}
                = render 'posts/read_rate', post: post
              （口コミ #{post.comments.count} 件）
