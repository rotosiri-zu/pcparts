.container
  .row
  - if @search.any?
    - @search.each do |post|
      .col-lg-3.col-md-6
        .card
          = image_tag post.image.url, class: "card-img-top"
          .card-body
            %h5.card-title= (post.title)
            %p.card-genre= (post.category.name)
            %span.card-price= "¥#{number_with_delimiter(post.price)}"
            .border.border-white.mt-4
              = link_to "詳細へ", post_path(post), class: "btn btn-primary" 
  - else
    %h1 検索に一致する商品はありませんでした